# Built only after bootstrap resumes the `secrets` Kustomization (see README.md);
# until then it may reference files that don't exist yet.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ghcr-pull.sops.yaml
